{
  "dependencies": {
    "github-markdown-css": "^5.8.1",
    "markdown-it": "^14.1.0"
  },
  "scripts": {
    "dev": "vite --open",
    "build": "vite build --port 5000",
    "preview": "vite preview --port 5000"
  }
}
